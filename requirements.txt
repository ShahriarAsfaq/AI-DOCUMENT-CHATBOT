# Core framework
Django>=4.2,<5

djangorestframework>=3.14

django-environ>=0.9

# RAG & embeddings
faiss-cpu>=1.7.3
sentence-transformers>=2.2.0
# CPU-only PyTorch wheel (Railway/Linux CPU)
# the +cpu suffix forces CPU build without CUDA/GPU dependencies
torch==2.1.1+cpu -f https://download.pytorch.org/whl/cpu/torch_stable.html

# LLM
groq>=0.4.0

# CORS
django-cors-headers>=3.15

# Document processing
PyMuPDF>=1.23.0
pypdf>=3.12.0
python-docx>=0.8.11

# OCR / images
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0

# LangChain for orchestration
langchain>=0.1.0
# note: langchain-community is not required for basic usage

# Async tasks
django-redis>=5.2
redis>=4.6
celery>=5.3

# Tokenizers (used by LangChain)
tiktoken>=0.6

# Production server
gunicorn>=20.1.0

# (development/test deps removed to slim install)



